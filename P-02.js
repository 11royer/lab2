// Definir la función que saluda a un usuario
function saludarUsuario(nombre) {
    console.log(`¡Hola, ${nombre}! Bienvenido.`);
}

// Llamar a la función con un nombre específico
saludarUsuario("ROYER");